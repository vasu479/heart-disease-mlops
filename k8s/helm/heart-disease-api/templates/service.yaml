apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-heart-disease-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-heart-disease-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
